#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    int a, n=1;
cout << endl << "n now is " << n << " at line " << 6 << endl;
    scanf("%d", &a);
{cout << endl << "a now is " << a << " at line " << 7 << endl;
}    for (int i = 1; i <= a; i++)
{cout << endl << "i now is " << i << " at line " << 8 << endl;
    {
        n = n * a % 100;
cout << endl << "n now is " << n << " at line " << 9 << endl;
}    }
    printf("%d\n", n);
}